name: My GraphQL task
slug: my_graphql_task
graphql:
  resource: ""
  operation: |-
    query GetUser($id: ID) {
      user(id: $id) {
        id
    	name
    	email
      }
    }
  variables:
    id: 1
  retryFailures: true
