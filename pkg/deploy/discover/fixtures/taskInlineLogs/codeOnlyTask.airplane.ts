import airplane from "airplane";

// Ensure that these logs don't mess up the parser.
console.log("foo:bar:bar:baz");
export const collatz = airplane.task(
  {
    slug: "collatz",
  },
  () => {}
);
